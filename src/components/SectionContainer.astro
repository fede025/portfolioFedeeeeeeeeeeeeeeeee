---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full px-4 sm:px-6 md:px-8 mx-auto max-w-full md:max-w-2xl lg:max-w-4xl`}
>
  <slot />
</section>